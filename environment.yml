# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: pacbio-preprocess-1.1
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - bioconda::fastqc
  - bioconda::multiqc=1.12
